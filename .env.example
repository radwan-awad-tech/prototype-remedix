# This frontend-only prototype does not require environment variables.
# Never place API credentials in VITE_* variables: browser bundles are public.
# Add secrets only to a future trusted server-side environment.
